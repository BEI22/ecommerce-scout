# ── LLM API 配置 ──────────────────────────────────────────
# 复制本文件为 .env 并填入你的密钥（.env 已被 .gitignore 排除，不会提交）

# DeepSeek（推荐，中文能力强、成本低）
DEEPSEEK_API_KEY=sk-在这里填入你的密钥

# OpenAI（可选，二选一即可）
# OPENAI_API_KEY=sk-在这里填入你的密钥

# Anthropic Claude（可选）
# ANTHROPIC_API_KEY=sk-ant-在这里填入你的密钥

# ── 说明 ──────────────────────────────────────────────────
# 三个密钥都不配置时，程序会自动降级到 Mock 模式：
#   - 所有功能流程完整可跑通
#   - 返回结构合理的模拟数据
#   - 零成本、可离线演示
#
# 配置优先级：ANTHROPIC_API_KEY > DEEPSEEK_API_KEY > OPENAI_API_KEY
